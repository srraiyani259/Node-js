<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Store Project</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Nunito", sans-serif;
      }

      body {
        background-color: #f0f2f5;
        color: #333;
      }

      h1 {
        margin-top: 50px;
        font-size: 32px;
        color: #2c3e50;
        letter-spacing: 1px;
      }

      .form-container {
        width: 60%;
        margin: 50px auto;
        padding: 20px;
        border-radius: 15px;
        background-color: #ffffff;
        box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
      }

      .form {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin-bottom: 30px;
      }

      input[type="text"],
      input[type="submit"] {
        width: 100%;
        padding: 15px;
        border-radius: 10px;
        border: 1px solid #bdc3c7;
        font-size: 16px;
      }

      input[type="text"] {
        background-color: #ecf0f1;
        color: #2c3e50;
      }

      input[type="text"]::placeholder {
        color: #95a5a6;
      }

      input[type="submit"] {
        background-color: #3498db;
        color: #fff;
        border: none;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      input[type="submit"]:hover {
        background-color: #2980b9;
      }

      .card-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
      }

      .card {
        flex: 1 1 calc(50% - 20px);
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
        display: flex;
        overflow: hidden;
        transition: transform 0.3s ease;
      }

      .card:hover {
        transform: scale(1.02);
      }

      .card-image {
        width: 30%;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #3498db;
      }

      .card-image img {
        width: 80px;
        height: 80px;
        cursor: pointer;
      }

      .card-book {
        width: 70%;
        padding: 20px;
      }

      .card-book h3 {
        color: #2c3e50;
        font-size: 20px;
        margin-bottom: 10px;
        text-transform: capitalize;
      }

      .card-book p {
        margin-bottom: 8px;
        color: #7f8c8d;
      }

      .card-book b {
        color: #34495e;
      }

      .card-buttons {
        display: flex;
        gap: 10px;
        margin-top: 15px;
      }

      .card-buttons button {
        flex: 1;
        padding: 10px;
        border-radius: 5px;
        border: none;
        font-size: 16px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      .delete {
        background-color: #e74c3c;
        color: #fff;
      }

      .delete:hover {
        background-color: #c0392b;
      }

      .edit {
        background-color: #2ecc71;
        color: #fff;
      }

      .edit:hover {
        background-color: #27ae60;
      }
    </style>
  </head>
  <body>
    <div class="form-container">
      <center>
        <h1>Book Store Project</h1>
      </center>

      <div class="form">
        <form action="/insert" method="post">
          <input type="text" name="Title" placeholder="Book Title : " required />
          <input type="text" name="Author" placeholder="Book Author Name : " required />
          <input type="text" name="Published" placeholder="Book Published Year : " required />
          <input type="text" name="Pages" placeholder="Book Pages :" required />
          <input type="text" name="Price" placeholder="Book Price : " required />
          <input type="text" name="Copies" placeholder="Book Published Copies : " required />
          <input type="submit" />
        </form>
      </div>

      <div class="card-container">
        <% BookData.map((e, i) => { %>
        <div class="card">
          <div class="card-image">
            <img src="https://cdn-icons-png.flaticon.com/512/14262/14262647.png" alt="Book Icon" />
          </div>
          <div class="card-book">
            <h3><%= e.Title %></h3>
            <p><b>Author : </b><%= e.Author %></p>
            <p><b>Published Year : </b><%= e.Published %></p>
            <p><b>Pages : </b><%= e.Pages %></p>
            <p><b>Price : </b><%= e.Price %> ₹</p>
            <p><b>Published Copies : </b><%= e.Copies %></p>
            <div class="card-buttons">
              <a href="/editData?id=<%= e.id %>" style="text-decoration: none;">
                <button class="edit">Edit</button>
              </a>
              <a href="/deleteData?id=<%= e.id %>" style="text-decoration: none;">
                <button class="delete">Delete</button>
              </a>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
  </body>
</html>

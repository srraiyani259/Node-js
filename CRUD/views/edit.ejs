<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Store Project</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Nunito", sans-serif;
      }

      body {
        background-color: #f0f2f5;
        color: #333;
      }

      .title{
        margin-bottom: 25px;
      }

      h1 {
        margin-top: 50px;
        font-size: 32px;
        color: #2c3e50;
        letter-spacing: 1px;
      }

      .form-container {
        width: 60%;
        margin: 50px auto;
        padding: 20px;
        border-radius: 15px;
        background-color: #ffffff;
        box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
      }

      .form {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin-bottom: 30px;
      }

      input[type="text"],
      input[type="submit"] {
        width: 100%;
        padding: 15px;
        border-radius: 10px;
        border: 1px solid #bdc3c7;
        font-size: 16px;
      }

      input[type="text"] {
        background-color: #ecf0f1;
        color: #2c3e50;
      }

      input[type="text"]::placeholder {
        color: #95a5a6;
      }

      input[type="submit"] {
        background-color: #3498db;
        color: #fff;
        border: none;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      input[type="submit"]:hover {
        background-color: #2980b9;
      }
    </style>
  </head>
  <body>
    <div class="form-container">
      <center class="title">
        <h1>Book Store Project Edit Page</h1>
      </center>
      <div class="form">
        <form action="/updateData?id=<%=singleBook._id%>" method="post">
          <input
            type="text"
            name="Title"
            value="<%=singleBook.Title%>"
            placeholder="Book Title : "
          />
          <input
            type="text"
            name="Author"
            value="<%=singleBook.Author%>"
            placeholder="Book Author Name : "
          />
          <input
            type="text"
            name="Published"
            value="<%=singleBook.Published%>"
            placeholder="Book Published Year : "
          />
          <input
            type="text"
            name="Pages"
            value="<%=singleBook.Pages%>"
            placeholder="Book Pages :"
          />
          <input
            type="text"
            name="Price"
            value="<%=singleBook.Price%>"
            placeholder="Book Price : "
          />
          <input
            type="text"
            name="Copies"
            value="<%=singleBook.Copies%>"
            placeholder="Book Published Copies : "
          />
          <br>
          <input type="submit" />
        </form>
      </div>
    </div>
  </body>
</html>
